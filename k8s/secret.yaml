apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  labels:
    app: mongo
type: Opaque
stringData:
  # Root credentials bootstrap MongoDB on first startup.
  root-password: SecureRootPass123
  # Application-scoped credentials used by the Flask pods.
  username: flaskuser
  password: SecureMongoPass123
